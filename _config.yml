# Site settings
title: CodeFolio
email: your-email@example.com
description: >- # this means to ignore newlines until "baseurl:"
  A modern Jekyll theme for showcasing software products
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site

# Theme settings
color_theme: "default"  # 全域預設顏色主題: default, ocean, emerald, sunset, rose, cyberpunk
show_color_theme_switcher: true  # 是否顯示顏色主題選擇器按鈕 (開發時設為 true，發布時設為 false)

# Build settings
markdown: kramdown
highlighter: rouge

# Include additional file types for Jekyll to serve
include:
  - _pages

# Keep video files and other assets
keep_files:
  - assets/videos
  - assets/images

# Plugins
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-paginate-v2
  - jemoji

# Multi-language support
languages: ["en", "zh-TW", "zh-CN"]
default_lang: "en"
exclude_from_localization: ["css", "js", "images", "assets"]

# Collections (模組化系統不再需要 collections)

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "default"

# Social media links
social:
  github: your-github
  twitter: your-twitter
  linkedin: your-linkedin
  email: your-email@example.com
